# Local Terraform directories
.terraform/

# Terraform state files (when using local state)
*.tfstate
*.tfstate.* 

# Terraform crash log files
crash.log

# Variable definition files (if sensitive data is present and not managed separately)
*.tfvars.json

# Terraform override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files (can contain sensitive details)
*.tfplan

# Terraform CLI configuration files
.terraformrc
terraform.rc

# Provider cache directory (if not within .terraform/)
.terraform.d/plugin-cache/